# ESP32-C5-LCD-1.47 硬件参考

本文档依据仓库内原理图整理，并与首方 Arduino 示例和 ESP-IDF 使用的板级 BSP
交叉核对。用于其他硬件修订版前，请先检查 `hardware/schematics/` 中的原理图。

## 核心硬件

| 功能 | 器件 / 接口 |
| --- | --- |
| MCU / Flash | ESP32-C5，内置 4 MB Flash。官方产品页标为 `ESP32-C5FH4`，仓库内原理图标为 `ESP32-C5HF4`，因此具体后缀属于待确认的源资料冲突。 |
| PSRAM | 官方产品页未列出、原理图也未显示板载 PSRAM；CI 明确将其禁用。 |
| 显示 | 1.47 英寸 ST7789，172 x 320 RGB565，SPI 接口 |
| 存储 | SPI microSD |
| 指示灯 | 一颗 WS2812B RGB LED |
| 触摸 | 本板不带触摸功能 |
| 其他板载外设 | 仓库内原理图未显示音频编解码器、扬声器/麦克风、RTC 或 I/O 扩展器。 |

## 显示信号

| 信号 | 分配 |
| --- | --- |
| SPI 时钟 | GPIO7 |
| SPI MOSI | GPIO6 |
| LCD 片选 | GPIO23 |
| LCD 数据/命令 | GPIO24 |
| LCD 复位 | GPIO26 |
| 背光 | GPIO10 |
| 示例 SPI 频率 | 40 MHz |
| 面板偏移 | X 34，Y 0 |

Arduino 示例使用横屏方向 3。修改旋转、面板偏移、色序或 SPI 频率后必须进行
真机验证。

## 存储与 RGB LED

| 信号 | 分配 |
| --- | --- |
| microSD 时钟 | GPIO7 |
| microSD MOSI | GPIO6 |
| microSD MISO | GPIO5 |
| microSD 片选 | GPIO4 |
| WS2812B 数据 | GPIO8 |

LCD 与 microSD 共用时钟和 MOSI，在两者之间切换时必须正确管理各自片选信号。

## 结构资料与单位

| 尺寸 | 图纸标注值 |
| --- | --- |
| LCD 有效显示区 | 17.39 x 32.35 mm |
| LCM 外形 | 19.39 +/- 0.2 x 36.28 +/- 0.2 mm |

带尺寸标注的 PDF 使用毫米量级，STEP 模型也声明为 SI 毫米。但仓库内 DXF
文件头同时声明 `$MEASUREMENT=0` 和 `$INSUNITS=1`（英寸），而其几何数据和
文字标注与上表毫米尺寸相符。若软件完全按 DXF 文件头导入，模型可能放大
25.4 倍。用于生产或外壳设计时，应将 DXF 导入单位明确覆盖为毫米，并先用
上表两个尺寸复核缩放比例。

STEP 装配体内的 PMMA 和胶粘件还保留了 `ESP32-S3-LCD-1_47` 历史名称；
顶层装配体和 PCBA 均标为 ESP32-C5。这些内部零件名不代表电路采用 S3，也不
表示本板具备 S3 产品的外设。

## 验证范围

以上分配已与仓库内硬件资料和源码核对。自动 CI 只能验证配置、编译和固件打包；
LCD 显示、背光范围、microSD 稳定性、RGB LED、无线功能及不同硬件修订版仍需
真机测试。使用机械尺寸时应交叉核对 `hardware/` 下的 PDF、DXF 和 STEP，不能
只依赖 DXF 文件头中的单位声明。
